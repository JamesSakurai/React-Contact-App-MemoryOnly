import React, { Component } from 'react';

import '../style/App.css'

export default class extends Component {
  render(){
    return(
      <h1 className="headerTitle">React Contacts App</h1>
    );
  }
}